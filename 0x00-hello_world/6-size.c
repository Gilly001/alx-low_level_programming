#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int a;
    long int b;
    long long int c;
    char d;
    float g;
    
    printf("size of interger:%lu bytes \n",(sizeof(a)));
    printf("size of long integer:%lu bytes \n", (sizeof(b)));
    printf("sizeof long long integer:%lu bytes \n", (sizeof(c)));
    printf("size of character: %lu bytes \n", (sizeof(d)));
    printf("size of float: %lu bytes \n", (sizeof(g)));
    return (0);
}#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int a;
    long int b;
    long long int c;
    char d;
    float g;
    
    printf("size of interger:%lu bytes \n",(sizeof(a)));
    printf("size of long integer:%lu bytes \n", (sizeof(b)));
    printf("sizeof long long integer:%lu bytes \n", (sizeof(c)));
    printf("size of character: %lu bytes \n", (sizeof(d)));
    printf("size of float: %lu bytes \n", (sizeof(g)));
    return (0);
}
